name: Generate Contributors

on:
  workflow_dispatch:
  # Refresh contributors after each release lands.
  workflow_run:
    workflows: ["Create release"]
    types:
      - completed

permissions:
  contents: write

jobs:
  update_contributors:
    uses: somaz94/.github/.github/workflows/contributors-reusable.yml@main
    secrets: inherit
